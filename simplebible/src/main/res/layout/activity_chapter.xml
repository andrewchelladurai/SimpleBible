<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <!--android:layout_marginBottom="?actionBarSize"-->

        <TextView
            style="@style/SbTheme.Header"
            android:id="@+id/act_chapter_titlebar"
            android:layout_margin="@dimen/gap_normal"
            android:text="@string/act_ch_title"
            android:textAlignment="center" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/act_chapter_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="@dimen/gap_normal"
            android:layout_weight="1"
            app:layoutManager="LinearLayoutManager"
            app:spanCount="@integer/act_chapter_list"
            tools:listitem="@layout/item_verse" />
    </LinearLayout>

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/act_chapter_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:backgroundTint="?colorPrimary"
        app:fabAlignmentMode="end"
        app:hideOnScroll="true" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/act_chapter_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_share"
        android:tint="?colorPrimary"
        app:layout_anchor="@id/act_chapter_appbar" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
