<?xml version="1.0" encoding="utf-8"?>
<menu>
    <!--
        <group android:checkableBehavior="single">
            <item
                android:id="@+id/nav_camera"
                android:app_icon="@drawable/ic_menu_camera"
                android:title="Import"/>
            <item
                android:id="@+id/nav_gallery"
                android:app_icon="@drawable/ic_menu_gallery"
                android:title="Gallery"/>
            <item
                android:id="@+id/nav_slideshow"
                android:app_icon="@drawable/ic_menu_slideshow"
                android:title="Slideshow"/>
            <item
                android:id="@+id/nav_manage"
                android:app_icon="@drawable/ic_menu_manage"
                android:title="Tools"/>
        </group>

        <item android:title="Communicate">
            <menu>
                <item
                    android:id="@+id/nav_share"
                    android:app_icon="@drawable/ic_menu_share"
                    android:title="Share"/>
                <item
                    android:id="@+id/nav_send"
                    android:app_icon="@drawable/ic_menu_send"
                    android:title="Send"/>
            </menu>
        </item>
    -->
</menu>
