<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.andrewchelladurai.simplebible"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ThemeLight">

        <activity
            android:name=".SimpleBible"
            android:label="@string/app_name"
            android:theme="@style/ThemeLight.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityChapterView"
            android:label="@string/title_activity_chapter_verse"
            android:parentActivityName=".SimpleBible"
            android:theme="@style/ThemeLight.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SimpleBible" />
        </activity>
        <activity
            android:name=".ActivityBookmarkedVerse"
            android:label="@string/title_activity_activity_bookmarked_verse"
            android:parentActivityName=".SimpleBible"
            android:theme="@style/ThemeLight.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.andrewchelladurai.simplebible.SimpleBible" />
        </activity>
    </application>

</manifest>
